# =============================================================================
# DEPRECATED — Dusky has migrated to Lua configuration
# =============================================================================
#
# This file is kept for reference only. Hyprland loads hyprland.lua instead
# (Lua takes priority over .conf when both are present).
#
# Do NOT edit this file. Edit hyprland.lua and the files it sources in:
#   ~/.config/hypr/source/        — base configuration
#   ~/.config/hypr/edit_here/     — your personal overrides
#
# The full Lua entry point is: ~/.config/hypr/hyprland.lua
# =============================================================================
